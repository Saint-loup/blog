<!DOCTYPE html>
<html lang="fr" class="font-sans">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ seo.title or title }}  | {{ meta.siteName }}</title>
    <link rel="stylesheet" href="{{ '/assets/tailwind.css' | url }}"/>
    <link rel="alternate" type="application/atom+xml" title="RSS Feed for {{ meta.siteName }}" href="{{ '/feed.xml' | url }}"/>
    {# Only include the Flickity js if this page uses a hero.carousel  #}
    {% if hero == "carousel" or product.carousel -%}
      <link rel="stylesheet" href="https://unpkg.com/flickity@2/dist/flickity.min.css">
      <link rel="stylesheet" href="https://unpkg.com/flickity-fullscreen@1/fullscreen.css">
    {%- endif -%}
    {%- include "partials/head.njk" %}
    {% set prismOkaidia %}{% include "src/assets/css/prism-okaidia.css" %}{% endset -%}
    <style>
      {{prismOkaidia | cssmin | safe}}
    </style>
  </head>
  <body x-init="
  document.body.style.setProperty('--navbar-height', document.querySelector('#navbar-mobile').children.length);
  "
  x-data="{ open: false}" class="template-{{layout}} {{ colors.siteTextCustom or siteTextDefault }} sans">
    {#    <div hidden>{% svgsprite %}</div>
#}
    <div class="flex flex-col min-h-screen mx-auto container-2xl">
      {% include "components/nav.njk" %}

      <main
      :class="{ 'anim-nav-closed': !open, 'anim-nav-opened': open }"
       class=" {{"sm:px-per-lg px-per " if layout == 'post'}} relative flex-grow block max-w-full mb-20 transition-all duration-300 ease-out " id="content">
        {{ content | safe }}
      </main>

      {% include "partials/footer.njk" %}
    </div>

    <script src=" {{ '/assets/main.js'   | url }}" defer></script>

    {# Only include the Flickity js if this page uses a hero.carousel  #}
    {% if hero == "carousel" or product.carousel %}
      <script src="https://unpkg.com/flickity@2/dist/flickity.pkgd.min.js"></script>
      <script src="https://unpkg.com/flickity-fullscreen@1/fullscreen.js"></script>
    {% endif %}

  </body>
</html>