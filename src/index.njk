---
title: Tout ce qui bouge
layout: base
date: 2020-08-05
excerpt: # used for page excerpts and META (will be overwritten if SEO used below)
eleventyNavigation: # Required if want to display in Main Nav Bar
  key: main # "main" is required
  title: Accueil # as it will appear in the nav
  order: 1 # order to display in the nav (index = 1)

---

<div class="{{ structure.g_pageWidth }}   ">
  <h1>Site en construction.</h1>

  <img style="margin:auto;width:30vmax;" src="/assets/images/construction.gif"></img>



  {% if seo.description %}
    <div class="p-2 {{ colors.borderCustom.borders or colors.borderDefault.borders }} {{ colors.borderCustom.color or colors.borderDefault.color }}">
      {{ seo.description }}
  {% endif %}

<h2>Projets, essais et autres bidouilles</h2>
<ul class="project-list " >
        {% for project in projects | reverse %}
          <li class="my-4 list-none project">
              <div class="text-base name"><a href="{{project.link}}">{{project.name}}</a></div>
              {#<img class="picture" src="{{project.img}}"></img>#}
              <div class="description">{{project.description}}</div>
            </li>
        {% endfor %}
</ul>

<h2>Moi, ailleurs</h2>
<ul class="ailleurs-list " >
        {% for item in ailleurs  %}
          <li class="my-4 list-none ailleurs">
              <div class="text-base name"><a href="{{item.link}}">{{item.name}}</a></div>
              {#<img class="picture" src="{{item.img}}"></img>#}
              <div class="description">{{item.description}}</div>
            </li>
        {% endfor %}
</ul>


</div>

